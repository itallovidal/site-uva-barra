## 1. Create the collaborators list page

- [x] 1.1 Create `src/pages/admin/collaborators-list-page.tsx` using `useCollaborators()` hook and `AdminColaboratorCard` with `variant="preview"`, with loading, error, and empty states
- [x] 1.2 Wire the new page into `src/routes/index.tsx` replacing the `PlaceholderPage` for `/admin/collaborators`
- [x] 1.3 Verify the page compiles and renders correctly with mock data
